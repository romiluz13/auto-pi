---
description: Tickets — break the approved spec into implementation tickets
argument-hint: "<spec reference>"
skill: to-tickets
---
Break the approved spec into implementation tickets. Follow the `to-tickets` skill (publish the approved slices as tickets).

Task: $@

Scope: the `to-tickets` skill includes a ticket-breakdown approval step (valid procedure-internal human gate). After the tickets are published, return the first unblocked ticket.

Next: type `/build <first unblocked ticket>`. You decide.
