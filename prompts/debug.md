---
description: Debug an issue — feedback loop, root cause (diagnosis only)
argument-hint: "<what's wrong>"
skill: orchestration-layer
---
Debug the following issue. The `orchestration-layer` skill will read `ask-matt` (the routing brain) and route to diagnosing-bugs for bugs/performance, or resolving-merge-conflicts for merge/rebase conflicts. State blocks + evidence gates + human-controlled stops are handled by the layer.

Issue: $@
