---
description: Research a topic — investigate against primary sources, capture findings as markdown
argument-hint: "<topic to research>"
skill: research
---
Research the following topic. Follow the `research` skill (investigate against high-trust primary sources, capture findings as a markdown file).

Topic: $@

Scope: supplement with parallel fan-out where useful — web (`bdata search`/`bdata scrape`), GitHub (`npx octocode`), codebase, memory. For code-focused research → `/skill:octocode`. For deep multi-source briefs → `/skill:live-research`. Do NOT write code — research only.

Next: type `/plan`, `/build`, or stop — based on findings. You decide.
