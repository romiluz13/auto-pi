---
description: Spec — publish the approved design to the issue tracker
argument-hint: "<approved design or spec topic>"
skill: to-spec
---
Publish the approved design to the issue tracker. Follow the `to-spec` skill (synthesize the approved conversation into a tracker spec).

Task: $@

Scope: the `to-spec` skill includes a user seam-confirmation step (valid procedure-internal human gate). After the spec is published, return the spec reference (issue URL or number).

Next: type `/tickets <spec reference>`. You decide.
