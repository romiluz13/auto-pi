---
description: Wayfind — chart a shared map for a foggy, large effort
argument-hint: "<the foggy effort>"
skill: wayfinder
---
Chart a shared map for a foggy, large effort. Follow the `wayfinder` skill (decisions across sessions, not deliverables — until the fog is pushed back and the way is clear).

Effort: $@

Scope: wayfinder is for the idea you can't hold in one session — too large or foggy for a single plan. It produces decisions/a map, not a spec. When the map's destination becomes clear, the next step is `/plan` or `/spec` (chosen from the actual outcome).

Next: type `/plan` (if the destination is now clear and bounded) or continue wayfinding. You decide.
