---
description: Review current diff — two-axis (standards + spec) + security, then disposition
skill: orchestration-layer
---
Review the current uncommitted changes. The `orchestration-layer` skill will read `ask-matt` (the routing brain) and orchestrate code-review → receiving-code-review, with security as a third reviewer. State blocks + evidence gates + human-controlled stops are handled by the layer.
