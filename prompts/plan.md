---
description: Plan a feature — understand, brainstorm, write spec + tickets
argument-hint: "<feature description>"
skill: brainstorming
---
Plan the following feature. Follow the `brainstorming` skill (understand → brainstorm → plan). This is planning only — do NOT write production code.

Feature: $@

Scope: the `brainstorming` skill transitions to `/skill:to-spec` then `/skill:to-tickets`. Stop after tickets are published.

Next: type `/build <ticket>`. You decide.
